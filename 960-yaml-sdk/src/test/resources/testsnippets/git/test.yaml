connector:

  # Name of the ConnectorDisconnectHandler Required
  name: "My git ConnectorDisconnectHandler"
  # Non-changeable identifier of the ConnectorDisconnectHandler, can not contain spaces or special chars. REQUIRED
  identifier: "my_git_connector"
  # Org ID
  orgIdentifier: "Devops"
  # Project ID
  projectIdentifier: "Harness_Sample_App"
  type: Git
  spec:
    connectionType: Account/Repo
    url: "ssh://github.com/harness/backend.git"
    branchName: master
    type: Http
    spec:
      username: "abhinav.singh@harness.io"
      passwordRef: "account.harnessAccountPassword"