integrations:
  github:
    - host: HOST_VALUE
      apiBaseUrl: https://HOST_VALUE/api/v3
      apps:
        - appId: ${HARNESS_GITHUB_APP_APPLICATION_ID}
          clientId: dummy
          clientSecret: dummy
          webhookSecret: dummy
          privateKey: |
            ${HARNESS_GITHUB_APP_PRIVATE_KEY_REF}

