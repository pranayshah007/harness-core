apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssca-manager.fullname" . }}
  labels:
    {{- include "ssca-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ssca-manager.selectorLabels" . | nindent 4 }}
