{{- if eq (include "harnesscommon.secrets.hasESOSecrets" (dict "secretsCtx" .Values.secrets)) "true" -}}
{{ include "harnesscommon.secrets.generateExtSecret" (dict "secretsCtx" .Values.secrets "secretNamePrefix"  (include "harness-manager.name" .)) }}
{{- end -}}