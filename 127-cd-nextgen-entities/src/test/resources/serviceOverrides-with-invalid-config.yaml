serviceOverrides:
  environmentRef: envId
  serviceRef: srvId
  variables: []
  configFiles:
    - configFile:
        identifier: c
        spec:
          store:
          type: Harness
          spec:
            files:
              - anyfile